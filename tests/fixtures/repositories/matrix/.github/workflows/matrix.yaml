name: Matrix
on: workflow_dispatch
jobs:
  test:
    strategy:
      matrix:
        python-version:
          - "3.12"
          - "3.13"
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ matrix.python-version }}"
