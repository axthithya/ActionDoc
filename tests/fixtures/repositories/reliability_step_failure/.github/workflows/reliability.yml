name: Ignored step failure
on: workflow_dispatch
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Optional lint
        run: exit 1
        continue-on-error: true
