name: Release
on: workflow_dispatch
permissions:
  contents: read
jobs:
  release:
    name: Release
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Run release
        run: echo "release"
